http {
  proxy-host = "127.0.0.1"
  proxy-host = ${?PROXY_HOST}

  proxy-port = 8888
  proxy-port = ${?PROXY_PORT}

  max-total-connections = 4096
  max-total-connections = ${?HTTP_MAX_TOTAL_CONNECTIONS}

  max-total-connections-per-host = 2048
  max-total-connections-per-host = ${?HTTP_MAX_TOTAL_CONNECTIONS_PER_HOST}

  max-wait-queue-limit = 256
  max-wait-queue-limit = ${?HTTP_MAX_WAIT_QUEUE_LIMIT}

  idle-timeout = 2 minutes
  idle-timeout = ${?HTTP_IDLE_TIMEOUT}

  connect-timeout = 8 seconds
  connect-timeout = ${?HTTP_CONNECT_TIMEOUT}

  request-timeout = 16 seconds
  request-timeout = ${?HTTP_REQUEST_TIMEOUT}

  request-max-total-attempts = 8
  request-max-total-attempts = ${?HTTP_REQUEST_MAX_TOTAL_ATTEMPTS}

  request-max-delay-between-attempts = 40 milliseconds
  request-max-delay-between-attempts = ${?HTTP_REQUEST_MAX_DELAY_BETWEEN_ATTEMPTS}
}

kafka {
  # A list of host/port pairs to use for establishing the initial connection to the Kafka cluster.
  bootstrap-servers = "localhost:9092"
  bootstrap-servers = ${?KAFKA_BOOTSTRAP_SERVERS}
}

schema-registry {
  # Comma-separated list of URLs for schema registry instances that can be used to register or look up schemas.
  url = "http://localhost:8081"
  url = ${?SCHEMA_REGISTRY_BASE_URL}
}

kafka-producer {
  # The topic to which the record should be produced.
  topic {
    commands = "marketplace_handler-commands-handle_ozon_request-version_2"
  }

  # This configuration controls the default batch size in bytes.
  batch-size = 16384
  batch-size = ${?SCHEDULER_KAFKA_PRODUCER_BATCH_SIZE}

  # This setting gives the upper bound on the delay for batching.
  linger = 50 milliseconds
  linger = ${?SCHEDULER_KAFKA_PRODUCER_LINGER}

  # FS2 Kafka. The max number of [[ProducerRecords]] to produce in the same batch when using the produce pipe.
  parallelism = 1024
  parallelism = ${?SCHEDULER_KAFKA_PRODUCER_PARALLELISM}
}

sources = [
  { type = ozon-seller, page-limit = 500, every = 50 minutes }

  { type = ozon-category, id = 6000,  search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 6500,  search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 7000,  search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 7500,  search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 8000,  search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 8500,  search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 9000,  search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 9200,  search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 9700,  search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 10500, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 11000, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 12300, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 13100, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 13300, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 13500, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 14500, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 14572, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 15000, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 15500, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 16500, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 18000, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 25000, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 32056, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 33332, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 34452, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 35659, search-filter = brand, every = 30 minutes }
  { type = ozon-category, id = 50001, search-filter = brand, every = 30 minutes }
]

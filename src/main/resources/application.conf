http {
  proxy-host = "127.0.0.1"
  proxy-host = ${?PROXY_HOST}

  proxy-port = 8888
  proxy-port = ${?PROXY_PORT}

  request-timeout = 5 seconds
  request-timeout = ${?HTTP_REQUEST_TIMEOUT}

  max-connections = 50000
  max-connections = ${?HTTP_MAX_CONNECTIONS}

  max-connections-per-host = 10000
  max-connections-per-host = ${?HTTP_MAX_CONNECTIONS_PER_HOST}
}

clickhouse {
  url = "jdbc:clickhouse://localhost:8123/"
  url = ${?CLICKHOUSE_URL}

  user = "default"
  user = ${?CLICKHOUSE_USER}

  pass = ""
  pass = ${?CLICKHOUSE_PASSWORD}

  thread-pool-size = 16
}

kafka {
  bootstrap-servers = "localhost:9092"
  bootstrap-servers = ${?KAFKA_BOOTSTRAP_SERVERS}
}

schema-registry {
  base-url = "http://localhost:8081"
  base-url = ${?SCHEMA_REGISTRY_BASE_URL}
}

parser {
  kafka-consumer {
    group-id = "marketplace_parser"
    group-id = ${?PARSER_KAFKA_CONSUMER_GROUP_ID}

    topic = "markeplace_crawler-events-ozon_request_handled-version_1"
    topic = ${?PARSER_KAFKA_CONSUMER_TOPIC}

    batch-offsets = 50
    batch-offsets = ${?PARSER_KAFKA_CONSUMER_BATCH_OFFSETS}

    batch-time-window = 100 milliseconds
    batch-time-window = ${?PARSER_KAFKA_CONSUMER_BATCH_TIME_WINDOW}
  }

  kafka-producer {
    topic = "marketplace_parser-results-ozon_category_search_results_v2_items-version_1"
    topic = ${?PARSER_KAFKA_PRODUCER_TOPIC}

    max-buffer-size = 1000
    max-buffer-size = ${?PARSER_KAFKA_PRODUCER_MAX_BUFFER_SIZE}

    linger = 10 milliseconds
    linger = ${?PARSER_KAFKA_PRODUCER_LINGER}

    batch-size = 16384
    batch-size = ${?PARSER_KAFKA_PRODUCER_BATCH_SIZE}
  }
}

crawler {
  max-connections-per-partition = 50
  max-connections-per-partition = ${?CRAWLER_MAX_CONNECTIONS}

  kafka-consumer {
    group-id = "marketplace_crawler"
    group-id = ${?CRAWLER_KAFKA_CONSUMER_GROUP_ID}

    topic = "marketplace_crawler-commands-handle_ozon_request-version_1"
    topic = ${?CRAWLER_KAFKA_CONSUMER_TOPIC}

    batch-offsets = 50
    batch-offsets = ${?CRAWLER_KAFKA_CONSUMER_BATCH_OFFSETS}

    batch-time-window = 100 milliseconds
    batch-time-window = ${?CRAWLER_KAFKA_CONSUMER_BATCH_TIME_WINDOW}
  }

  kafka-producer {
    topic = "markeplace_crawler-events-ozon_request_handled-version_1"
    topic = ${?CRAWLER_KAFKA_PRODUCER_TOPIC}

    compression-type = "zstd"
    compression-type = ${?CRAWLER_KAFKA_PRODUCER_COMPRESSION_TYPE}

    max-buffer-size = 1000
    max-buffer-size = ${?CRAWLER_KAFKA_PRODUCER_MAX_BUFFER_SIZE}

    linger = 100 milliseconds
    linger = ${?CRAWLER_KAFKA_PRODUCER_LINGER}

    batch-size = 655360
    batch-size = ${?CRAWLER_KAFKA_PRODUCER_BATCH_SIZE}
  }
}

scheduler {
  kafka-producer {
    topic = "marketplace_crawler-commands-handle_ozon_request-version_1"
    topic = ${?SCHEDULER_KAFKA_PRODUCER_TOPIC}

    max-buffer-size = 1000
    max-buffer-size = ${?SCHEDULER_KAFKA_PRODUCER_MAX_BUFFER_SIZE}

    linger = 0 milliseconds
    linger = ${?SCHEDULER_KAFKA_PRODUCER_LINGER}

    batch-size = 16384
    batch-size = ${?SCHEDULER_KAFKA_PRODUCER_BATCH_SIZE}
  }
}

sources = [
  { type = ozon-category, id = 6000,  every = 60 minutes }
  { type = ozon-category, id = 6500,  every = 60 minutes }
  { type = ozon-category, id = 7000,  every = 60 minutes }
  { type = ozon-category, id = 7500,  every = 60 minutes }
  { type = ozon-category, id = 8000,  every = 60 minutes }
  { type = ozon-category, id = 8500,  every = 60 minutes }
  { type = ozon-category, id = 9000,  every = 60 minutes }
  { type = ozon-category, id = 9200,  every = 60 minutes }
  { type = ozon-category, id = 9700,  every = 60 minutes }
  { type = ozon-category, id = 10500, every = 60 minutes }
  { type = ozon-category, id = 11000, every = 60 minutes }
  { type = ozon-category, id = 12300, every = 60 minutes }
  { type = ozon-category, id = 13100, every = 60 minutes }
  { type = ozon-category, id = 13300, every = 60 minutes }
  { type = ozon-category, id = 13500, every = 60 minutes }
  { type = ozon-category, id = 14500, every = 60 minutes }
  { type = ozon-category, id = 14572, every = 60 minutes }
  { type = ozon-category, id = 15000, every = 60 minutes }
  { type = ozon-category, id = 15500, every = 60 minutes }
  { type = ozon-category, id = 16500, every = 60 minutes }
  { type = ozon-category, id = 18000, every = 60 minutes }
  { type = ozon-category, id = 25000, every = 60 minutes }
  { type = ozon-category, id = 32056, every = 60 minutes }
  { type = ozon-category, id = 33332, every = 60 minutes }
  { type = ozon-category, id = 34452, every = 60 minutes }
  { type = ozon-category, id = 35659, every = 60 minutes }
  { type = ozon-category, id = 50001, every = 60 minutes }
]

